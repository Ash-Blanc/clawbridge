---
title: Memory & Storage
---

# Memory

Configured in the agent's YAML.

```yaml
storage:
  enabled: true
  type: sqlite # in_memory | sqlite | postgres
  db_url: memory.db
```

Memory is natively handled by Agno `Db` providers.
